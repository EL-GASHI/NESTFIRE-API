<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reset Your Password</title>
  <link rel="stylesheet" href="/styles.css"> <!-- Assuming you have a global stylesheet -->
</head>
<body>
  <div class="container">
    <h1 class="greeting">Reset Your Password</h1>
    <p class="instruction">Enter a new password below.</p>

    <form action="/api/auth/reset-password" method="POST" class="form-container">
      <input type="hidden" name="email" value="<%= email %>"> <!-- Pass email dynamically from route -->
      
      <label for="newPassword" class="label">New Password</label>
      <input
        type="password"
        name="newPassword"
        id="newPassword"
        class="input <%= errors.newPassword ? 'input-error' : '' %>"
        placeholder="New Password"
        required
      />
      <% if (errors.newPassword) { %>
        <p class="error-text"><%= errors.newPassword %></p>
      <% } %>

      <label for="confirmPassword" class="label">Confirm New Password</label>
      <input
        type="password"
        name="confirmPassword"
        id="confirmPassword"
        class="input <%= errors.confirmPassword ? 'input-error' : '' %>"
        placeholder="Confirm New Password"
        required
      />
      <% if (errors.confirmPassword) { %>
        <p class="error-text"><%= errors.confirmPassword %></p>
      <% } %>

      <button type="submit" class="button">Reset Password</button>
    </form>

    <p class="link">
      Remember your password? <a href="/login">Log in here</a>
    </p>
  </div>
</body>
</html>
